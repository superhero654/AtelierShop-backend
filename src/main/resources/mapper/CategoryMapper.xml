<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.su.ateliershop.mapper.CategoryMapper">
    <select id="findAll" resultType="com.su.ateliershop.entity.Category">
        SELECT * FROM category ORDER BY id
    </select>
    <select id="findById" resultType="com.su.ateliershop.entity.Category">
        SELECT * FROM category WHERE id = #{id}
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO category (name, icon, description) VALUES (#{name}, #{icon}, #{description})
    </insert>
    <update id="update">
        UPDATE category SET name = #{name}, icon = #{icon}, description = #{description} WHERE id = #{id}
    </update>
    <delete id="deleteById">
        DELETE FROM category WHERE id = #{id}
    </delete>
</mapper>
