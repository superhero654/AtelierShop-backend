<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.su.ateliershop.mapper.OrderItemMapper">
    <select id="findByOrderId" resultType="com.su.ateliershop.entity.OrderItem">
        SELECT id, order_id AS orderId, good_id AS goodId, count, price
        FROM order_item WHERE order_id = #{orderId}
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_item (order_id, good_id, count, price)
        VALUES (#{orderId}, #{goodId}, #{count}, #{price})
    </insert>
    <delete id="deleteByOrderId">
        DELETE FROM order_item WHERE order_id = #{orderId}
    </delete>
</mapper>
